# ── Unreal Engine — transient / generated (also matches nested plugin dirs) ──
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# ── Visual Studio / Rider / IDE ──
.vs/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
.idea/

# ── OS noise ──
.DS_Store
Thumbs.db

# ── Megascans / Fab library assets (large, re-importable; kept local, not tracked) ──
/Content/Megascans/
/Content/Fab/
/Content/Developers/

# ── Imported asset / tutorial packs (large, re-importable; kept local, not tracked) ──
/Content/Ancient_Pillars/
/Content/TWW_LevelDesign/
/Content/MSPresets/
/Content/GemstoneShaders/

# ── Python tooling cache ──
__pycache__/
